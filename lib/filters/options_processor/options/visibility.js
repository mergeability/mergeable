class Visibility {
  static process (context, filter, settings) {
    // TODO do something
    return {
      status: 'fail',
      description: 'because I want to'
    }
  }
}

module.exports = Visibility
